malloc str to print (like before)
send this str to function that's goint to bufferise and do tampon
in this function every ms, send buffer to write function (need to create a thread)

PSEUDO CODE:

static char *buf;

get_delta_time();
tmp = ft_str_join(buf, new_str)
